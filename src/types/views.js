// @flow

export type CarView = {
    stockNumber: number,
    title: string,
    image: string,
    link: string,
    description: string,
    isFavourite: boolean
};

export type SelectView = {
    title: string,
    value: string
};
